const STUDENTS = [
      {
        name: "Asya Hassan",
        class: "1A",
        username: "ahassan",
        password: "58241",
        role: "student",
        scores: {
          week1: 0,
          // later: week2: ..., week3: ...
        }
      },
      {
        name: "Asel Ismail",
        class: "1A",
        username: "aismail",
        password: "93706",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Aylan Talal",
        class: "1A",
        username: "atalal",
        password: "41058",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Tala Mohamed",
        class: "1A",
        username: "tmohamed",
        password: "26937",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Zeina Samer ",
        class: "1A",
        username: "zsamer",
        password: "84513",
        role: "student",
        scores: {
          week1: 25,
        }
      },
      {
        name: "Fatma Mohamed",
        class: "1A",
        username: "fmohamed",
        password: "70329",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Layan Khaled",
        class: "1A",
        username: "lkhaled",
        password: "19684",
        role: "student",
        scores: {
          week1: 10,
        }
      },
      {
        name: "Yasmin Ehab",
        class: "1A",
        username: "yehab",
        password: "52897",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Ahmed Talal",
        class: "1A",
        username: "atalal",
        password: "64072",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Khaled Waleed",
        class: "1A",
        username: "kwaleed",
        password: "64072",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Fayad Raed",
        class: "1A",
        username: "fraed",
        password: "87402",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Malek Mohamed",
        class: "1A",
        username: "mmohamed",
        password: "95268",
        role: "student",
        scores: {
          week1: 10,
        }
      },
      {
        name: "Mobasher Megahed",
        class: "1A",
        username: "mmegahed",
        password: "73145",
        role: "student",
        scores: {
          week1: 9,
        }
      },
      {
        name: "Yahia Ahmed",
        class: "1A",
        username: "yahmed",
        password: "20489",
        role: "student",
        scores: {
          week1: 19,
        }
      },
      {
        name: "Gamila Ahmed",
        class: "1B",
        username: "gahmed",
        password: "11835",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Ganna Mohamed",
        class: "1B",
        username: "gmohamed",
        password: "22764",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Dana Ahmed",
        class: "1B",
        username: "dahmed",
        password: "33271",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Dima Mohamed",
        class: "1B",
        username: "dmohamed",
        password: "44986",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Zat Elmuiz",
        class: "1B",
        username: "zelmuiz",
        password: "55310",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Rana Youssef",
        class: "1B",
        username: "ryoussef",
        password: "66742",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Fayrouz Mohamed",
        class: "1B",
        username: "fmohamed",
        password: "77193",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Carla Hossam",
        class: "1B",
        username: "chossam",
        password: "88924",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Adam ahmed",
        class: "1B",
        username: "aahmed",
        password: "91537",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Adam Amr",
        class: "1B",
        username: "aamr",
        password: "24619",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Adham Nasr",
        class: "1B",
        username: "anasr",
        password: "38420",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Rahim Amr",
        class: "1B",
        username: "ramr",
        password: "59276",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Abdelrahman Sherif",
        class: "1B",
        username: "asherif",
        password: "61853",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Mohamed Hasanien",
        class: "1B",
        username: "mhasanien",
        password: "76401",
        role: "student",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Younis Yasser",
        class: "1B",
        username: "yyasser",
        password: "89652",
        role: "student",
        scores: {
          week1: 0,
        }
      },

{ username: "fmohamed ", password: "15853", name: "Farida mohamed ", class: "2A", role: "student", scores: { week1: 0 } },
{ username: "habdelmoiem", password: "26616", name: "Hala Abdelmoiem", class: "2A", role: "student", scores: { week1: 0 } },
{ username: "kmohamed", password: "40650", name: "Karma Mohamed", class: "2A", role: "student", scores: { week1: 0 } },
{ username: "kmostafa", password: "29143", name: "Karma Mohamed Mostafa", class: "2A", role: "student", scores: { week1: 0 } },
{ username: "kmostafa", password: "28176", name: "Karma Mostafa", class: "2A", role: "student", scores: { week1: 0 } },
{ username: "mmahmoud", password: "58545", name: "Maria Mahmoud", class: "2A", role: "student", scores: { week1: 0 } },
{ username: "rhussien", password: "59912", name: "Rawnag Hussien", class: "2A", role: "student", scores: { week1: 0 } },
{ username: "smarwan ", password: "93241", name: "Syan Marwan ", class: "2A", role: "student", scores: { week1: 0 } },
{ username: "mmahmoud", password: "58580", name: "Malika Mahmoud", class: "2A", role: "student", scores: { week1: 0 } },
{ username: "aseif", password: "95826", name: "Abdelrahman Seif", class: "2A", role: "student", scores: { week1: 0 } },
{ username: "ahussien ", password: "21717", name: "Ahmed Hussien ", class: "2A", role: "student", scores: { week1: 0 } },
{ username: "ashehab", password: "88537", name: "Ahmed Shehab", class: "2A", role: "student", scores: { week1: 0 } },
{ username: "malaa", password: "72722", name: "Malek Alaa", class: "2A", role: "student", scores: { week1: 0 } },
{ username: "nadam", password: "25451", name: "Nael Adam", class: "2A", role: "student", scores: { week1: 0 } },
{ username: "omohamed", password: "53703", name: "Omar Mohamed", class: "2A", role: "student", scores: { week1: 0 } },
{ username: "zaldin", password: "55568", name: "Zain Aldin", class: "2A", role: "student", scores: { week1: 0 } },
      
    
{ username: "amontser", password: "95264", name: "Amira Montser", class: "2B", role: "student", scores: { week1: 0 } },
{ username: "hwalid ", password: "16655", name: "Hayat Walid ", class: "2B", role: "student", scores: { week1: 0 } },
{ username: "mmostafa", password: "68940", name: "Makka Mostafa", class: "2B", role: "student", scores: { week1: 0 } },
{ username: "tyousry", password: "99058", name: "Tala Yousry", class: "2B", role: "student", scores: { week1: 0 } },
{ username: "rashraf", password: "16234", name: "Reman Ashraf", class: "2B", role: "student", scores: { week1: 0 } },
{ username: "fabdelrahman", password: "50794", name: "Fahd Abdelrahman", class: "2B", role: "student", scores: { week1: 0 } },
{ username: "hseody", password: "88575", name: "Hamza Seody", class: "2B", role: "student", scores: { week1: 0 } },
{ username: "mamr", password: "43859", name: "Malek Amr", class: "2B", role: "student", scores: { week1: 0 } },
{ username: "mahmed ", password: "14189", name: "Mohamed Ahmed ", class: "2B", role: "student", scores: { week1: 0 } },
{ username: "mali ", password: "14599", name: "Mohamed Ali ", class: "2B", role: "student", scores: { week1: 0 } },
{ username: "mkarim ", password: "95801", name: "Mohamed Karim ", class: "2B", role: "student", scores: { week1: 0 } },
{ username: "mmostafa", password: "49447", name: "Mohamed Mostafa", class: "2B", role: "student", scores: { week1: 0 } },
{ username: "m moataz", password: "31236", name: "Mohamed  Moataz", class: "2B", role: "student", scores: { week1: 0 } },
{ username: "rislam", password: "71377", name: "Rayan Islam", class: "2B", role: "student", scores: { week1: 0 } },
{ username: "shossain", password: "28880", name: "Sajed Hossain", class: "2B", role: "student", scores: { week1: 0 } },
{ username: "sgamal", password: "64323", name: "Selim Gamal", class: "2B", role: "student", scores: { week1: 0 } },

{ username: "atamer ", password: "42580", name: "Aseel Tamer ", class: "3", role: "student", scores: { week1: 0 } },
{ username: "aahmed ", password: "76790", name: "Ayla Ahmed ", class: "3", role: "student", scores: { week1: 0 } },
{ username: "bahmed", password: "64003", name: "Berla Mohamed Ahmed ", class: "3", role: "student", scores: { week1: 0 } },
{ username: "badel", password: "40017", name: "Berla Mohamed Adel", class: "3", role: "student", scores: { week1: 14 } },
{ username: "gali ", password: "42841", name: "Gowan Ali ", class: "3", role: "student", scores: { week1: 0 } },
{ username: "hothman ", password: "58144", name: "Hoor Othman ", class: "3", role: "student", scores: { week1: 0 } },
{ username: "rahmed ", password: "29734", name: "Rovan Ahmed ", class: "3", role: "student", scores: { week1: 0 } },
{ username: "kramy ", password: "78436", name: "Karma Ramy ", class: "3", role: "student", scores: { week1: 0 } },
{ username: "amostafa", password: "60778", name: "Asser Mostafa", class: "3", role: "student", scores: { week1: 0 } },
{ username: "iabdelmaboud", password: "30208", name: "Ibrahim Abdelmaboud", class: "3", role: "student", scores: { week1: 0 } },
{ username: "eislam", password: "53872", name: "Eyad Islam", class: "3", role: "student", scores: { week1: 20 } },
{ username: "tallah", password: "25687", name: "Taim Allah", class: "3", role: "student", scores: { week1: 0 } },
{ username: "tsherif", password: "62701", name: "Tamim Sherif", class: "3", role: "student", scores: { week1: 20 } },
{ username: "hmohamed", password: "76116", name: "Hamza Mohamed", class: "3", role: "student", scores: { week1: 0 } },
{ username: "amina ", password: "69796", name: "Abdallah Mina ", class: "3", role: "student", scores: { week1: 0 } },
{ username: "aahmed", password: "33293", name: "Amr Ahmed", class: "3", role: "student", scores: { week1: 0 } },
{ username: "mreda", password: "19211", name: "Mohamed Reda", class: "3", role: "student", scores: { week1: 0 } },

{ username: "aahmed ", password: "44605", name: "Asia Ahmed ", class: "4", role: "student", scores: { week1: 0 } },
{ username: "aamer", password: "27237", name: "Alma Amer", class: "4", role: "student", scores: { week1: 0 } },
{ username: "hmontser", password: "79926", name: "Hanin Montser", class: "4", role: "student", scores: { week1: 0 } },
{ username: "jhassan", password: "39992", name: "Jana Hassan", class: "4", role: "student", scores: { week1: 0 } },
{ username: "hmohamed", password: "90812", name: "Hayat Mohamed", class: "4", role: "student", scores: { week1: 0 } },
{ username: "fabdelrahman", password: "13062", name: "Farah Abdelrahman", class: "4", role: "student", scores: { week1: 0 } },
{ username: "kshehab", password: "41207", name: "Karma Shehab", class: "4", role: "student", scores: { week1: 0 } },
{ username: "mreda", password: "87790", name: "Malka Reda", class: "4", role: "student", scores: { week1: 0 } },
{ username: "amahmoud ", password: "51994", name: "Ahmed Mahmoud ", class: "4", role: "student", scores: { week1: 0 } },
{ username: "amostafa", password: "77509", name: "Adam Mostafa", class: "4", role: "student", scores: { week1: 0 } },
{ username: "hahmed ", password: "65276", name: "Hamza Ahmed ", class: "4", role: "student", scores: { week1: 0 } },
{ username: "amohsen", password: "92483", name: "Abdallah Mohsen", class: "4", role: "student", scores: { week1: 0 } },
{ username: "ohossain", password: "22486", name: "Omar hossain", class: "4", role: "student", scores: { week1: 0 } },
{ username: "smostafa", password: "19424", name: "Slim Mostafa", class: "4", role: "student", scores: { week1: 0 } },
{ username: "mahmed ", password: "58771", name: "Mohamed Ahmed ", class: "4", role: "student", scores: { week1: 0 } },
{ username: "mahmed ali", password: "89107", name: "Mohamed Ahmed Ali", class: "4", role: "student", scores: { week1: 0 } },
{ username: "mosama ", password: "31415", name: "Megdam Osama ", class: "4", role: "student", scores: { week1: 0 } },
{ username: "ymohamed", password: "34516", name: "Yassin Mohamed", class: "4", role: "student", scores: { week1: 0 } },

{ username: "jabdelraziq ", password: "52912", name: "Judy Abdelraziq ", class: "5A", role: "student", scores: { week1: 0 } },
{ username: "dmarwan", password: "70556", name: "Dan Marwan", class: "5A", role: "student", scores: { week1: 0 } },
{ username: "rabdelrahman", password: "86718", name: "Rahma Abdelrahman", class: "5A", role: "student", scores: { week1: 0 } },
{ username: "rsherif ", password: "72156", name: "Retag Sherif ", class: "5A", role: "student", scores: { week1: 0 } },
{ username: "chamdy", password: "65582", name: "Celia Hamdy", class: "5A", role: "student", scores: { week1: 0 } },
{ username: "fyasser", password: "15431", name: "Farha Yasser", class: "5A", role: "student", scores: { week1: 0 } },
{ username: "fahmed ", password: "94394", name: "Farida Ahmed ", class: "5A", role: "student", scores: { week1: 0 } },
{ username: "kahmed", password: "74072", name: "Kinda Ahmed", class: "5A", role: "student", scores: { week1: 0 } },
{ username: "ymaher", password: "20841", name: "Yasmin Maher", class: "5A", role: "student", scores: { week1: 0 } },
{ username: "kayman", password: "39989", name: "Khaled Ayman", class: "5A", role: "student", scores: { week1: 0 } },
{ username: "sislam", password: "57405", name: "Shehab Islam", class: "5A", role: "student", scores: { week1: 0 } },
{ username: "mmohamed", password: "22065", name: "Malek Mohamed", class: "5A", role: "student", scores: { week1: 0 } },
{ username: "mhany ", password: "54608", name: "Mohamed Hany ", class: "5A", role: "student", scores: { week1: 0 } },

{ username: "ahossain ", password: "68673", name: "Ayten Hossain ", class: "5B", role: "student", scores: { week1: 0 } },
{ username: "bmoaaz", password: "43981", name: "Brihan Moaaz", class: "5B", role: "student", scores: { week1: 0 } },
{ username: "tmohamed", password: "27746", name: "Talia Mohamed", class: "5B", role: "student", scores: { week1: 0 } },
{ username: "mahmed ", password: "76716", name: "Mariam Ahmed ", class: "5B", role: "student", scores: { week1: 0 } },
{ username: "ahany", password: "61664", name: "Ahmed Hany", class: "5B", role: "student", scores: { week1: 0 } },
{ username: "ayousry ", password: "33785", name: "Ahmed Yousry ", class: "5B", role: "student", scores: { week1: 0 } },
{ username: "abarakat", password: "64268", name: "Abdallah Barakat", class: "5B", role: "student", scores: { week1: 0 } },
{ username: "oahmed ", password: "54234", name: "Omar Ahmed ", class: "5B", role: "student", scores: { week1: 0 } },
{ username: "omahmoud", password: "64540", name: "Omar Mahmoud", class: "5B", role: "student", scores: { week1: 0 } },
{ username: "smohamed ", password: "19580", name: "Saif Mohamed ", class: "5B", role: "student", scores: { week1: 0 } },
{ username: "mosama ", password: "23162", name: "Moaaz Osama ", class: "5B", role: "student", scores: { week1: 0 } },
{ username: "ymosaab", password: "47781", name: "Youssef Mosaab", class: "5B", role: "student", scores: { week1: 0 } },

{ username: "aahmed ", password: "92810", name: "Alma Ahmed ", class: "6", role: "student", scores: { week1: 16 } },
{ username: "jmostafa", password: "89471", name: "Jana Mostafa", class: "6", role: "student", scores: { week1: 0 } },
{ username: "rahmed ", password: "52364", name: "Rafeef Ahmed ", class: "6", role: "student", scores: { week1: 19 } },
{ username: "zmohamed", password: "92867", name: "Zeina Mohamed", class: "6", role: "student", scores: { week1: 19 } },
{ username: "mhassan ", password: "77410", name: "Malak Hassan ", class: "6", role: "student", scores: { week1: 0 } },
{ username: "mallah", password: "19955", name: "Mennat Allah", class: "6", role: "student", scores: { week1: 0 } },
{ username: "imahmoud", password: "74741", name: "Ibrahim mahmoud", class: "6", role: "student", scores: { week1: 0 } },
{ username: "aamin ", password: "78781", name: "Abdallah Amin ", class: "6", role: "student", scores: { week1: 0 } },
{ username: "mabelrahman", password: "26555", name: "Mohamed Abelrahman", class: "6", role: "student", scores: { week1: 22 } },
{ username: "mabdelrahman", password: "36460", name: "Mostafa Abdelrahman", class: "6", role: "student", scores: { week1: 16 } },
{ username: "mosman", password: "10124", name: "Moyasser Osman", class: "6", role: "student", scores: { week1: 0 } },
{ username: "yahmed ", password: "79771", name: "Yassin Ahmed ", class: "6", role: "student", scores: { week1: 25 } },
{ username: "ymohamed", password: "72249", name: "Youssef Mohamed", class: "6", role: "student", scores: { week1: 17 } },
{ username: "aabdallah", password: "55814", name: "Aya Abdallah", class: "6", role: "student", scores: { week1: 0 } },

{ username: "ahatem ", password: "96794", name: "Aysel Hatem ", class: "7", role: "student", scores: { week1: 0 } },
{ username: "jahmed ", password: "55377", name: "Jana Ahmed ", class: "7", role: "student", scores: { week1: 20 } },
{ username: "rahmed ", password: "91195", name: "Rawan Ahmed ", class: "7", role: "student", scores: { week1: 0 } },
{ username: "stamer ", password: "24465", name: "Sila Tamer ", class: "7", role: "student", scores: { week1: 0 } },
{ username: "mahmed ", password: "26308", name: "Mariam Ahmed ", class: "7", role: "student", scores: { week1: 0 } },
{ username: "mhassan ", password: "46714", name: "Mariam Hassan ", class: "7", role: "student", scores: { week1: 0 } },
{ username: "nabdelfatah", password: "74840", name: "Nada Abdelfatah", class: "7", role: "student", scores: { week1: 21 } },
{ username: "wmahmoud", password: "24766", name: "Wajen mahmoud", class: "7", role: "student", scores: { week1: 0 } },
{ username: "aamin ", password: "40059", name: "ahmed Amin ", class: "7", role: "student", scores: { week1: 0 } },
{ username: "aahmed", password: "48477", name: "Asser Ahmed", class: "7", role: "student", scores: { week1: 0 } },
{ username: "ahisham", password: "14314", name: "Amir Hisham", class: "7", role: "student", scores: { week1: 0 } },
{ username: "bmohamed ", password: "62586", name: "Badr Mohamed ", class: "7", role: "student", scores: { week1: 0 } },
{ username: "bmoaaz", password: "50507", name: "Braa Moaaz", class: "7", role: "student", scores: { week1: 18 } },
{ username: "htarek", password: "69543", name: "Hamza Tarek", class: "7", role: "student", scores: { week1: 25 } },
{ username: "myousry", password: "57823", name: "Mazen Yousry", class: "7", role: "student", scores: { week1: 0 } },
{ username: "mmaher", password: "23510", name: "Mostafa Maher", class: "7", role: "student", scores: { week1: 0 } },
{ username: "ayasser", password: "10083", name: "Ammar Yasser", class: "7", role: "student", scores: { week1: 0 } },
{ username: "olotfy", password: "61814", name: "Omar Lotfy", class: "7", role: "student", scores: { week1: 0 } },
{ username: "yosama", password: "94389", name: "Yassien Osama", class: "7", role: "student", scores: { week1: 0 } },
{ username: "ymohamed", password: "56349", name: "Youssef Mohamed", class: "7", role: "student", scores: { week1: 0 } },
{ username: "ymostafa", password: "33167", name: "Youssef Mostafa", class: "7", role: "student", scores: { week1: 0 } },

{ username: "aahmed ", password: "37869", name: "Arwa Ahmed ", class: "8", role: "student", scores: { week1: 0 } },
{ username: "aehab", password: "48328", name: "Aseel Ehab", class: "8", role: "student", scores: { week1: 0 } },
{ username: "ehossain", password: "85491", name: "Esraa Hossain", class: "8", role: "student", scores: { week1: 0 } },
{ username: "ralaa", password: "86181", name: "Roaa Alaa", class: "8", role: "student", scores: { week1: 0 } },
{ username: "rahmed", password: "59879", name: "Roqaia Ahmed", class: "8", role: "student", scores: { week1: 20 } },
{ username: "shany ", password: "93369", name: "Salma Hany ", class: "8", role: "student", scores: { week1: 0 } },
{ username: "sabdallah", password: "10384", name: "Safia Abdallah", class: "8", role: "student", scores: { week1: 0 } },
{ username: "lmohamed ", password: "42204", name: "Lamar Mohamed ", class: "8", role: "student", scores: { week1: 20 } },
{ username: "losama ", password: "39737", name: "Logeen Osama ", class: "8", role: "student", scores: { week1: 0 } },
{ username: "mzaher", password: "73325", name: "Maya Zaher", class: "8", role: "student", scores: { week1: 0 } },
{ username: "nahmed ", password: "15186", name: "Nadeen Ahmed ", class: "8", role: "student", scores: { week1: 0 } },
{ username: "aahmed ", password: "50389", name: "Adam Ahmed ", class: "8", role: "student", scores: { week1: 0 } },
{ username: "amohamed", password: "42080", name: "Anas Mohamed", class: "8", role: "student", scores: { week1: 23 } },
{ username: "hahmed ", password: "10906", name: "Hamza Ahmed ", class: "8", role: "student", scores: { week1: 0 } },
{ username: "hosama ", password: "29966", name: "Hamza Osama ", class: "8", role: "student", scores: { week1: 0 } },
{ username: "abarakat", password: "33090", name: "Abdelrahman Barakat", class: "8", role: "student", scores: { week1: 0 } },
{ username: "omontaser", password: "73562", name: "Omar Montaser", class: "8", role: "student", scores: { week1: 0 } },
{ username: "mabdelmaboud", password: "12306", name: "Mahmoud Abdelmaboud", class: "8", role: "student", scores: { week1: 0 } },
{ username: "mahmed", password: "95140", name: "Mohsen Ahmed", class: "8", role: "student", scores: { week1: 0 } },
{ username: "nabdelrahman", password: "12687", name: "Nabil Abdelrahman", class: "8", role: "student", scores: { week1: 0 } },
{ username: "yhisham", password: "77616", name: "Youssef Hisham", class: "8", role: "student", scores: { week1: 0 } },

{ username: "hamin", password: "74475", name: "Hala Amin", class: "9", role: "student", scores: { week1: 0 } },
{ username: "ryoussef ", password: "96026", name: "Rodaina Youssef ", class: "9", role: "student", scores: { week1: 20 } },
{ username: "ryoussef ", password: "48384", name: "Raghad Youssef ", class: "9", role: "student", scores: { week1: 26 } },
{ username: "rahmed ", password: "58489", name: "Rital Ahmed ", class: "9", role: "student", scores: { week1: 24 } },
{ username: "fothman ", password: "52142", name: "Farah Othman ", class: "9", role: "student", scores: { week1: 25 } },
{ username: "mmohamed", password: "80521", name: "Menna Mohamed", class: "9", role: "student", scores: { week1: 0 } },
{ username: "ahatem", password: "73484", name: "Asser Hatem", class: "9", role: "student", scores: { week1: 0 } },


      {
        name: "Omar Mohamed",
        class: "STAFF",
        username: "omohamed",
        password: "2",
        role: "teacher",
        scores: {
          week1: 0,
        }
      },
      {
        name: "Marwa Samy",
        class: "SUPERVISOR",
        username: "msamy",
        password: "1",
        role: "teacher",
        scores: {
          week1: 0,
        }
      },
        // TODO: add all your other students here in the same format
      // {
      //   name: "Khaled Walid",
      //   class: "7",
      //   scores: { week1: 20 }
      // },
      // {
      //   name: "Layan Khaled",
      //   class: "4",
      //   scores: { week1: 25 }
      // }
    ];
// ============================
//
// TODO: paste your big students array here from your old main.js.
// Change the first line from:
//   const students = [
//
// to:
//   const STUDENTS = [
//
// Make sure the array ends with ];
//
// Example shape of ONE student (your real data is longer):
//
// const STUDENTS = [
//   {
//     name: "Asya Hassan",
//     class: "1A",
//     username: "ahassan",
//     password: "58241",
//     role: "student",
//     scores: { week1: 0 }
//   },
//   ...
// ];
// ============================


// ============================
// 2. AUTH HELPERS (LOGIN DATA)
// ============================

const PRESENTACY_USER_KEY = "presentacy_current_user";

function presentacySetCurrentUser(account) {
  if (!account) return;
  const data = {
    username: (account.username || "").trim().toLowerCase(),
    name: account.name || "",
    class: account.class || "",
    role: account.role || "student"
  };
  try {
    localStorage.setItem(PRESENTACY_USER_KEY, JSON.stringify(data));
  } catch (e) {
    console.error("Could not save user", e);
  }
}

function presentacyGetCurrentUser() {
  try {
    const raw = localStorage.getItem(PRESENTACY_USER_KEY);
    if (!raw) return null;
    return JSON.parse(raw);
  } catch (e) {
    console.error("Could not read user", e);
    return null;
  }
}

function presentacyLogout() {
  try {
    localStorage.removeItem(PRESENTACY_USER_KEY);
  } catch (e) {
    // ignore
  }
  window.location.href = "login.html";
}

function setupLogoutButton() {
  const logoutLink = document.getElementById("logout-link");
  if (!logoutLink) return;
  logoutLink.addEventListener("click", (e) => {
    e.preventDefault();
    presentacyLogout();
  });
}

// ============================
// 3. LOGIN PAGE LOGIC
// ============================

function initPresentacyLoginPage() {
  const form = document.getElementById("login-form");
  if (!form) return;

  const usernameInput = document.getElementById("login-username");
  const passwordInput = document.getElementById("login-password");
  const errorEl = document.getElementById("login-error");

  // Already logged in? Go to leaderboard
  const existing = presentacyGetCurrentUser();
  if (existing) {
    window.location.href = "leaderboard.html";
    return;
  }

  form.addEventListener("submit", (event) => {
    event.preventDefault();

    const username = (usernameInput.value || "").trim().toLowerCase();
    const password = (passwordInput.value || "").trim();

    if (!username || !password) {
      if (errorEl) {
        errorEl.textContent = "Please fill both username and password.";
      }
      return;
    }

    if (!Array.isArray(STUDENTS)) {
      console.error("STUDENTS array is missing!");
      if (errorEl) {
        errorEl.textContent = "Internal error – tell your teacher.";
      }
      return;
    }

    // Match ignoring extra spaces in stored usernames
    const account = STUDENTS.find((s) => {
      const storedUser = (s.username || "").trim().toLowerCase();
      return storedUser === username && String(s.password) === password;
    });

    if (!account) {
      if (errorEl) {
        errorEl.textContent = "Username or password is wrong. Try again.";
      }
      return;
    }

    if (errorEl) errorEl.textContent = "";

    presentacySetCurrentUser(account);
    window.location.href = "leaderboard.html";
  });
}

// ============================
// 4. LEADERBOARD HELPERS
// ============================

// Sum of all weeks for leaderboard
function getTotalPoints(student) {
  const scores = student.scores || {};
  return Object.values(scores).reduce(
    (sum, value) => sum + (typeof value === "number" ? value : 0),
    0
  );
}

function getBadgeForPoints(points) {
  if (points >= 65) return "Presentacy Star";
  if (points >= 55) return "Shining Star";
  if (points >= 30) return "Bright Star";
  return "Rising Star";
}

// Build the leaderboard table (top 5) for whole school or for a class
function renderLeaderboardTable(classFilter) {
  const tbody = document.getElementById("leaderboard-table-body");
  if (!tbody || !Array.isArray(STUDENTS)) return;

  let pool =
    classFilter && classFilter !== ""
      ? STUDENTS.filter((s) => s.class === classFilter)
      : STUDENTS.slice();

  // Remove non-students (teachers)
  pool = pool.filter((s) => s.role !== "teacher");

  const withTotals = pool.map((s) => ({
    ...s,
    totalPoints: getTotalPoints(s)
  }));

  const nonZero = withTotals.filter((s) => s.totalPoints > 0);

  nonZero.sort((a, b) => b.totalPoints - a.totalPoints);

  const top = nonZero.slice(0, 5);

  tbody.innerHTML = top
    .map((student, index) => {
      const rank = index + 1;
      const rankClass =
        rank === 1
          ? "rank rank-1"
          : rank === 2
          ? "rank rank-2"
          : rank === 3
          ? "rank rank-3"
          : "rank";
      const badge = getBadgeForPoints(student.totalPoints);

      return `
        <tr>
          <td class="${rankClass}">${rank}</td>
          <td>${student.name}</td>
          <td>${student.class}</td>
          <td>${student.totalPoints}</td>
          <td><span class="badge">${badge}</span></td>
        </tr>
      `;
    })
    .join("");

  if (top.length === 0) {
    tbody.innerHTML = `
      <tr>
        <td colspan="5" style="font-size:0.85rem; color:var(--text-muted); padding:0.75rem;">
          No scores yet. The leaderboard will appear after the first presentations.
        </td>
      </tr>
    `;
  }
}

function getMotivationText(scoreWeek1) {
  const score = typeof scoreWeek1 === "number" ? scoreWeek1 : 0;

  if (score >= 28) {
    return "Amazing start! Keep this level and try to challenge yourself with harder topics.";
  } else if (score >= 20) {
    return "Great work. You have a solid base—focus on one skill (like eye contact or voice) to level up.";
  } else if (score > 0) {
    return "You’ve started the journey. Every presentation is practice—keep going and you will feel the difference.";
  } else {
    return "Your first score will appear here after your first presentation. Get ready to show us what you can do!";
  }
}

function renderStudentResultCard(student, targetEl) {
  if (!targetEl) return;

  if (!student) {
    targetEl.innerHTML = `
      <p class="student-message">
        No matching student was found. Please check the name and class.
      </p>
    `;
    return;
  }

  const scores = student.scores || {};
  const weekEntries = Object.entries(scores);

  weekEntries.sort((a, b) => {
    const numA = parseInt(a[0].replace("week", ""), 10) || 0;
    const numB = parseInt(b[0].replace("week", ""), 10) || 0;
    return numA - numB;
  });

  let weeksHtml = "";

  if (weekEntries.length === 0) {
    weeksHtml = "<p>No scores recorded yet.</p>";
  } else {
    weeksHtml = weekEntries
      .map(([weekKey, score]) => {
        const weekNumber = weekKey.replace("week", "");
        return `<p>Week ${weekNumber}: <strong>${score}</strong> points</p>`;
      })
      .join("");
  }

  const week1Score = scores.week1 || 0;
  const motivation = getMotivationText(week1Score);

  targetEl.innerHTML = `
    <div class="student-result-card">
      <h3>${student.name}</h3>
      <div class="student-meta">
        Class: ${student.class}
      </div>
      <div class="student-weeks">
        ${weeksHtml}
      </div>
      <p class="motivation">${motivation}</p>
    </div>
  `;
}

function renderMultipleResults(list, term, selectedClassLabel, targetEl) {
  if (!targetEl) return;

  const cards = list
    .map(
      (student) => `
      <div class="student-result-card">
        <h3>${student.name}</h3>
        <div class="student-meta">
          Class: ${student.class}
        </div>
        <div class="student-weeks">
          ${
            student.scores && typeof student.scores.week1 === "number"
              ? `<p>Week 1: <strong>${student.scores.week1}</strong> points</p>`
              : "<p>No scores recorded yet.</p>"
          }
        </div>
      </div>
    `
    )
    .join("");

  const classInfo =
    selectedClassLabel && selectedClassLabel !== ""
      ? ` in class <strong>${selectedClassLabel}</strong>`
      : "";

  targetEl.innerHTML = `
    <div style="margin-bottom:0.4rem; font-size:0.85rem; color:var(--text-muted);">
      We found ${list.length} students matching "<strong>${term}</strong>"${classInfo}. 
      Add more letters (for example last name) if this is not you.
    </div>
    <div class="student-multi-results">
      ${cards}
    </div>
  `;
}

function renderNotFound(term, selectedClassLabel, targetEl) {
  if (!targetEl) return;
  const classInfo =
    selectedClassLabel && selectedClassLabel !== ""
      ? ` in class "<strong>${selectedClassLabel}</strong>"`
      : "";

  targetEl.innerHTML = `
    <div class="error-message">
      No student found with the name "<strong>${term}</strong>"${classInfo}. 
      Check your spelling or try your full name.
    </div>
  `;
}

// ============================
// 5. LEADERBOARD SEARCH MODES
// ============================

function setupTeacherSearchOnLeaderboard() {
  const classFilter = document.getElementById("class-filter");
  const nameInput = document.getElementById("student-search-input");
  const searchBtn = document.getElementById("student-search-button");
  const resultEl = document.getElementById("student-result");

  if (!classFilter || !nameInput || !searchBtn || !resultEl) return;

  classFilter.disabled = false;
  nameInput.disabled = false;
  searchBtn.disabled = false;
  searchBtn.textContent = "Search";

  searchBtn.addEventListener("click", () => {
    const selectedClass = classFilter.value.trim();
    const term = nameInput.value.trim();
    if (!term) {
      resultEl.innerHTML =
        '<p class="student-message">Please type a name to search.</p>';
      return;
    }

    const lowerTerm = term.toLowerCase();

    let pool = STUDENTS.filter((s) => s.role === "student");
    if (selectedClass) {
      pool = pool.filter((s) => s.class === selectedClass);
    }

    if (pool.length === 0) {
      renderNotFound(term, selectedClass, resultEl);
      return;
    }

    const exact = pool.find(
      (s) => s.name.toLowerCase() === lowerTerm
    );
    if (exact) {
      renderStudentResultCard(exact, resultEl);
      return;
    }

    const startsWith = pool.filter((s) => {
      const n = s.name.toLowerCase();
      return n === lowerTerm || n.startsWith(lowerTerm + " ");
    });

    if (startsWith.length === 1) {
      renderStudentResultCard(startsWith[0], resultEl);
      return;
    } else if (startsWith.length > 1) {
      renderMultipleResults(startsWith, term, selectedClass, resultEl);
      return;
    }

    const broad = pool.filter((s) =>
      s.name.toLowerCase().includes(lowerTerm)
    );

    if (broad.length === 1) {
      renderStudentResultCard(broad[0], resultEl);
    } else if (broad.length > 1) {
      renderMultipleResults(broad, term, selectedClass, resultEl);
    } else {
      renderNotFound(term, selectedClass, resultEl);
    }
  });
}

function setupStudentViewOnLeaderboard(currentUser) {
  const searchSection = document.getElementById("student-search");
  const resultEl = document.getElementById("student-result");

  if (!searchSection || !resultEl) return;

  const titleEl = searchSection.querySelector("h1");
  const infoPara = searchSection.querySelector("p");
  const searchRow = searchSection.querySelector(".search-row");

  // 1) Hide the whole search row (class, name, button)
  if (searchRow) {
    searchRow.style.display = "none";
  }

  // 2) Change the title & text to talk to this student
  if (titleEl) {
    titleEl.textContent = "Your Presentacy Score";
  }

  if (infoPara) {
    infoPara.textContent = `This card shows your score for each week, ${currentUser.name}.`;
  }

  // 3) Find this student in the data
  // 3) Find this student in the data – match username *and* password
  const me = STUDENTS.find((s) => {
    const su = (s.username || "").trim().toLowerCase();
    const cu = (currentUser.username || "").trim().toLowerCase();
    const sp = (s.password || "").trim();
    const cp = (currentUser.password || "").trim();

    return su === cu && sp === cp;
  });

  if (!me) {
    resultEl.innerHTML = `
      <p class="student-message">
        You are logged in as <strong>${currentUser.name}</strong>,
        but we couldn’t find your data. Please tell your teacher.
      </p>
    `;
    return;
  }

  // 4) Show a small friendly line + their score card
  resultEl.innerHTML = `
    <p class="student-message">
      Hi <strong>${me.name}</strong> (${me.class}) 👋  
      Here is your Presentacy score for each week:
    </p>
  `;
  renderStudentResultCard(me, resultEl);
}

function setupAnonymousViewOnLeaderboard() {
  const classFilter = document.getElementById("class-filter");
  const nameInput = document.getElementById("student-search-input");
  const searchBtn = document.getElementById("student-search-button");
  const resultEl = document.getElementById("student-result");

  if (!resultEl) return;

  if (classFilter) classFilter.disabled = true;
  if (nameInput) {
    nameInput.disabled = true;
    nameInput.placeholder = "Log in to see your own score.";
  }
  if (searchBtn) {
    searchBtn.disabled = true;
    searchBtn.textContent = "Login required";
  }

  resultEl.innerHTML = `
    <p class="student-message">
      Please log in on this device if you want to see your own score.
    </p>
  `;
}

function initLeaderboardPage() {
  // Leaderboard table
  const leaderboardClassFilter = document.getElementById(
    "leaderboard-class-filter"
  );
  const leaderboardBody = document.getElementById("leaderboard-table-body");

  if (leaderboardBody) {
    renderLeaderboardTable("");
    if (leaderboardClassFilter) {
      leaderboardClassFilter.addEventListener("change", () => {
        renderLeaderboardTable(leaderboardClassFilter.value);
      });
    }
  }

  // Search card behaviour depends on login
  const user = presentacyGetCurrentUser();
  if (!user) {
    setupAnonymousViewOnLeaderboard();
  } else if (user.role === "teacher") {
    setupTeacherSearchOnLeaderboard();
  } else {
    setupStudentViewOnLeaderboard(user);
  }
}

// ============================
// 6. NAVBAR + HOME + GENERATOR + WALL
// ============================

function setupNavHighlight() {
  const currentPage = document.body.dataset.page;
  const links = document.querySelectorAll("[data-page-link]");
  links.forEach((link) => {
    if (link.dataset.pageLink === currentPage) {
      link.classList.add("nav-link--active");
    }
  });
}

let mobileNavInitialized = false;

function setupMobileNav() {
  if (mobileNavInitialized) return; // prevent adding the listener twice
  mobileNavInitialized = true;

  const navToggle = document.querySelector(".nav-toggle");
  const navLinks = document.querySelector(".nav-links");
  if (!navToggle || !navLinks) return;

  navToggle.addEventListener("click", () => {
    navLinks.classList.toggle("nav-links--open");
  });
}

function renderUserGreeting() {
  const greetingEl = document.getElementById("user-greeting");
  if (!greetingEl) return;

  const user = presentacyGetCurrentUser();
  if (!user) {
    greetingEl.textContent = "";
    return;
  }

  const fullName = user.name || "";
  const firstName = fullName.split(" ")[0] || fullName;

  const hour = new Date().getHours();
  let prefix = "Welcome";
  if (hour < 12) {
    prefix = "Good morning";
  } else if (hour < 18) {
    prefix = "Good afternoon";
  } else {
    prefix = "Good evening";
  }

  greetingEl.textContent = `${prefix}, ${firstName}!`;
}

function initHomePage() {
  const quoteEl = document.getElementById("presentacy-quote");
  const challengeEl = document.getElementById("presentacy-challenge");

  if (!quoteEl || !challengeEl) return;

  const quotes = [
    "Every time you speak, you get 1% better.",
    "Your voice matters more than your mistakes.",
    "Confidence is just practice wearing a costume.",
    "The audience wants you to win, not fail.",
    "A strong start is good, but a clear ending is magic.",
    "If you’re nervous, that means you care. Use it.",
    "Fluency grows when you keep talking, not when you’re perfect."
  ];

  const challenges = [
    "In your next presentation, try to look at three different people in the room.",
    "Choose one key word and say it a little slower and stronger than the others.",
    "Start your next talk with a question instead of “Today I will talk about…”.",
    "Use one new word from your vocabulary notebook in your next presentation.",
    "Try not to read for 10 seconds: just look up and speak from your head.",
    "Add one short personal story to your next presentation.",
    "End your next talk with one clear sentence that starts with “So in the end…”."
  ];

  const randomQuote = quotes[Math.floor(Math.random() * quotes.length)];
  const randomChallenge =
    challenges[Math.floor(Math.random() * challenges.length)];

  quoteEl.textContent = randomQuote;
  challengeEl.textContent = randomChallenge;
}

function initGeneratorPage() {
  const topicButton = document.getElementById("topic-button");
  const topicOutput = document.getElementById("topic-output");

  if (!topicButton || !topicOutput) return;

  // Get current user (saved in localStorage after login)
  const currentUser =
    (typeof presentacyGetCurrentUser === "function" &&
      presentacyGetCurrentUser()) ||
    null;

  // Get first name if possible
  let firstName = "";
  if (currentUser) {
    if (typeof getFirstName === "function") {
      firstName = getFirstName(currentUser.name || currentUser.username);
    } else {
      firstName = (currentUser.name || currentUser.username || "").split(" ")[0];
    }
  }

  // Time-of-day greeting (morning / afternoon / evening)
  function getTimeGreeting() {
    const hour = new Date().getHours();
    if (hour < 12) return "Good morning";
    if (hour < 18) return "Good afternoon";
    return "Good evening";
  }

  // Set the initial text before the button is clicked
  if (firstName) {
    topicOutput.textContent = `Hi ${firstName}! Press the button to get your topic.`;
  } else {
    topicOutput.textContent = "Press the button to get your first topic.";
  }

  const topics = [
    "My dream school day",
    "A superpower I would love to have",
    "The best trip I have ever taken",
    "If I could meet any famous person",
    "A hobby that makes me feel calm",
    "The most interesting animal in the world",
    "One invention that changed our lives",
    "If I could design my own bedroom",
    "A book or story that stayed with me",
    "The perfect weekend for me",
    "A time when I helped someone",
    "If I could live in any country",
    "Why we should be kind online",
    "One rule I would add to our school",
    "A job I might like in the future",
    "Why teamwork is important",
    "A place in nature I would like to visit",
    "Something I learned from a mistake",
    "My favorite memory with friends",
    "If I could travel to the future",
    "A food everyone should try at least once",
    "Why sleep is more powerful than we think",
    "The most impressive building or place I have seen",
    "What makes a good friend",
    "One small change that could improve our classroom"
  ];

  topicButton.addEventListener("click", () => {
    const randomTopic = topics[Math.floor(Math.random() * topics.length)];

    if (firstName) {
      const greeting = getTimeGreeting();
      // Personalized sentence talking to the student
      topicOutput.textContent = `${greeting}, ${firstName}! How about you talk about “${randomTopic}”?`;
    } else {
      // Fallback if somehow no user is found
      topicOutput.textContent = randomTopic;
    }
  });
}

function initPresentacyWallPage() {
  const wallTabButtons = document.querySelectorAll(".wall-tab-button");
  const wallPanels = document.querySelectorAll(".wall-tab-panel");

  if (!wallTabButtons.length || !wallPanels.length) return;

  wallTabButtons.forEach((btn) => {
    btn.addEventListener("click", () => {
      const target = btn.dataset.wallTab;

      wallTabButtons.forEach((b) =>
        b.classList.remove("wall-tab-button--active")
      );
      btn.classList.add("wall-tab-button--active");

      wallPanels.forEach((panel) => {
        const isActive = panel.dataset.wallPanel === target;
        panel.classList.toggle("wall-tab-panel--active", isActive);
      });
    });
  });
}

/* ========================
   GREETING BANNER
   ======================== */

function renderUserGreeting() {
  const banner = document.getElementById("greeting-banner");
  if (!banner) return;

  const user = presentacyGetCurrentUser && presentacyGetCurrentUser();
  // No user = no greeting
  if (!user) {
    banner.classList.remove("greeting-banner--visible");
    banner.innerHTML = "";
    return;
  }

  const fullName = user.name || "";
  const firstName = fullName.split(" ")[0] || fullName;

  const mainLines = [
    `Hey ${firstName}, ready to show your voice again?`,
    `${firstName}, your next great presentation is loading…`,
    `Welcome back, ${firstName}. Let’s make this week 1% better.`,
    `${firstName}, your audience is waiting for you 👀`,
    `Hi ${firstName}! Presentacy is happy you’re here.`
  ];

  const subLines = [
    "Every presentation is practice, not a performance.",
    "Your ideas matter more than perfect grammar.",
    "Nervous is normal. Brave is talking anyway.",
    "Tiny improvements every week become big changes.",
    "You don’t have to be perfect, just a bit clearer than last time."
  ];

  const main =
    mainLines[Math.floor(Math.random() * mainLines.length)];
  const sub =
    subLines[Math.floor(Math.random() * subLines.length)];

  const initial = firstName.charAt(0).toUpperCase();

  banner.innerHTML = `
    <div class="greeting-avatar">${initial}</div>
    <div>
      <div class="greeting-text-main">${main}</div>
      <div class="greeting-text-sub">${sub}</div>
    </div>
  `;

  requestAnimationFrame(() => {
    banner.classList.add("greeting-banner--visible");
  });
}

/* ========================
   STUDENT STORY HELPERS
   ======================== */

function getMotivationText(week1Score) {
  if (week1Score === 0) {
    return "You haven’t presented yet. Your score will appear after your first presentation.";
  }
  if (week1Score >= 24) {
    return "Amazing work. Keep challenging yourself with tougher topics and stronger stories.";
  }
  if (week1Score >= 16) {
    return "Strong start. Choose one skill to push higher next time – eye contact, body language, or voice.";
  }
  if (week1Score >= 8) {
    return "Nice first steps. Focus on one small change next presentation and your score will grow quickly.";
  }
  return "You were brave enough to stand and speak. That’s the hardest part – keep going.";
}

function getStudentProgressStory(student) {
  const scores = student.scores || {};
  const entries = Object.entries(scores).filter(
    ([, value]) => typeof value === "number"
  );

  if (entries.length === 0) {
    return "Your first score will appear here after your first presentation. Get ready.";
  }

  if (entries.length === 1) {
    const weekKey = entries[0][0];
    const weekNumber = weekKey.replace("week", "");
    const score = entries[0][1];
    return `You’ve completed your first Presentacy in week ${weekNumber} with ${score} points. Great start — the next step is even more important.`;
  }

  entries.sort((a, b) => {
    const numA = parseInt(a[0].replace("week", ""), 10) || 0;
    const numB = parseInt(b[0].replace("week", ""), 10) || 0;
    return numA - numB;
  });

  const firstScore = entries[0][1];
  const lastScore = entries[entries.length - 1][1];

  if (lastScore > firstScore) {
    return `You’ve presented ${entries.length} times so far, and your score went up from ${firstScore} to ${lastScore}. That’s called progress — keep going.`;
  } else if (lastScore < firstScore) {
    return `You’ve presented ${entries.length} times. This week was a bit lower (${lastScore} vs ${firstScore}), but one score doesn’t define you. Learn and try again.`;
  } else {
    return `You’ve presented ${entries.length} times so far. Your score stayed steady at ${lastScore}; now try one new thing (like stronger eye contact or clearer voice).`;
  }
}

// ===============================
//  STUDENT RESULT CARD
// ===============================
function renderStudentResultCard(student, targetEl) {
  if (!targetEl) return;

  if (!student) {
    targetEl.innerHTML = `
      <p class="student-message">
        No matching student was found. Please check the name and class.
      </p>
    `;
    return;
  }

  const scores = student.scores || {};
  const weekEntries = Object.entries(scores);

  // Sort by week number (week1, week2, ...)
  weekEntries.sort((a, b) => {
    const numA = parseInt(a[0].replace("week", ""), 10) || 0;
    const numB = parseInt(b[0].replace("week", ""), 10) || 0;
    return numA - numB;
  });

  let weeksHtml = "";

  if (weekEntries.length === 0) {
    weeksHtml = `<p>No scores recorded yet.</p>`;
  } else {
    weeksHtml = weekEntries
      .map(([weekKey, score]) => {
        const weekNumber = weekKey.replace("week", "");
        return `<p>Week ${weekNumber}: <strong>${score}</strong> points</p>`;
      })
      .join("");
  }

  // Week 1 score for the motivation line
  const week1Score =
    typeof scores.week1 === "number" ? scores.week1 : 0;
  const motivation = getMotivationText(week1Score);

  targetEl.innerHTML = `
    <div class="student-result-card">
      <div class="student-result-header">
        <div class="student-result-name">${student.name}</div>
        <div class="student-result-class">${student.class}</div>
      </div>

      <div class="student-weeks">
        ${weeksHtml}
      </div>

      <p class="motivation">${motivation}</p>
    </div>
  `;
}

/* ========================
   STUDENT VIEW ON LEADERBOARD
   ======================== */

function setupStudentViewOnLeaderboard(currentUser) {
  const searchSection = document.getElementById("student-search");
  const resultEl = document.getElementById("student-result");

  if (!searchSection || !resultEl) return;

  const titleEl = searchSection.querySelector("h1");
  const infoPara = searchSection.querySelector("p");
  const searchRow = searchSection.querySelector(".search-row");

  // Hide the search (class + name + button) for students
  if (searchRow) {
    searchRow.style.display = "none";
  }

  if (titleEl) {
    titleEl.textContent = "Your Presentacy Score";
  }

  if (infoPara) {
    infoPara.textContent = `This card shows your score for each week, ${currentUser.name}.`;
  }

  const me = STUDENTS.find(
    (s) =>
      (s.username || "").trim().toLowerCase() ===
      (currentUser.username || "").trim().toLowerCase()
  );

  if (!me) {
    resultEl.innerHTML = `
      <p class="student-message">
        You are logged in as <strong>${currentUser.name}</strong>,
        but we couldn’t find your data. Please tell your teacher.
      </p>
    `;
    return;
  }

  resultEl.innerHTML = `
    <p class="student-message">
      Hi <strong>${me.name}</strong> (${me.class}) 👋  
      Here is your Presentacy score for each week:
    </p>
  `;
  renderStudentResultCard(me, resultEl);
}

/* ========================
   WORDLE REACTION
   ======================== */

function presentacyShowWordleReaction(isWin, solutionWord) {
  const banner = document.getElementById("wordle-reaction");
  if (!banner) return;

  const user = presentacyGetCurrentUser && presentacyGetCurrentUser();
  const fullName = user?.name || "";
  const firstName = fullName.split(" ")[0] || "presenter";

  let emoji;
  let text;
  let extraClass;

  if (isWin) {
    emoji = "🎉";
    extraClass = "reaction-banner--success";
    text = `Nice one, ${firstName}! You cracked today’s word: ${solutionWord.toUpperCase()}. One small win for your brain, one big win for your Presentacy.`;
  } else {
    emoji = "💪";
    extraClass = "reaction-banner--fail";
    text = `Almost there, ${firstName}. You didn’t get today’s word (${solutionWord.toUpperCase()}), but showing up again tomorrow is what really trains your speaking brain.`;
  }

  banner.className = "reaction-banner reaction-banner--visible " + extraClass;
  banner.innerHTML = `
    <span class="reaction-emoji">${emoji}</span>
    <span>${text}</span>
  `;
}

function showWordleWinMessage(secretWord) {
  const msgEl = document.getElementById("wordle-message");
  if (!msgEl) return;

  const user = getPresentacyCurrentUser();
  const first = user ? getFirstName(user.name || user.username) : "Presentacy star";

  msgEl.textContent = `Great job, ${first}! You found today’s word: ${secretWord}.`;
  msgEl.classList.add("wordle-message-success");
}

/* ========================
   VIDEO REACTIONS
   ======================== */

function initVideoReactions() {
  const cards = document.querySelectorAll(".video-card");
  if (!cards.length) return;

  const user = presentacyGetCurrentUser && presentacyGetCurrentUser();
  const fullName = user?.name || "";
  const firstName = fullName.split(" ")[0] || "presenter";

  cards.forEach((card) => {
    const label = card.getAttribute("data-video-label") || "this video";
    const button = card.querySelector(".video-watch-btn");
    const reactionEl = card.querySelector(".video-reaction");
    if (!button || !reactionEl) return;

    button.addEventListener("click", () => {
      reactionEl.innerHTML = `
        ✅ Nice choice, <strong>${firstName}</strong>. 
        Try one idea from <em>${label}</em> in your next presentation.
      `;
    });
  });
}

/* ========================
   DOMContentLoaded – FINAL
   ======================== */

document.addEventListener("DOMContentLoaded", () => {
  const page = document.body.getAttribute("data-page") || "";

  // Common for all pages
  if (typeof setupNavHighlight === "function") setupNavHighlight();
  if (typeof setupMobileNav === "function") setupMobileNav();
  if (typeof setupLogoutButton === "function") setupLogoutButton();

  // Home hero stuff
  if (typeof updateHomeHeroCard === "function") updateHomeHeroCard();
  if (typeof updateHomeHeroCard === "function") {
  updateHomeHeroCard();
}

  // Page-specific inits
  if (typeof initHomePage === "function") initHomePage();
  if (typeof initGeneratorPage === "function") initGeneratorPage();
  if (typeof initPresentacyWallPage === "function") initPresentacyWallPage();

  if (page === "login" && typeof initPresentacyLoginPage === "function") {
    initPresentacyLoginPage();
    return;
  }

  if (page === "leaderboard" && typeof initLeaderboardPage === "function") {
    initLeaderboardPage();
  }

  if (page === "videos" && typeof initVideoReactions === "function") {
    initVideoReactions();
  }
});

// ============================
// 7. ONE CLEAN DOMContentLoaded
// ============================
function getFirstName(fullName) {
  if (!fullName) return "";
  return fullName.trim().split(/\s+/)[0];
}

// Returns "First Last" (first two names) for the badge
function getFirstTwoNames(fullName) {
  if (!fullName) return "";
  const parts = fullName.trim().split(/\s+/);
  if (parts.length === 1) return parts[0];
  return parts[0] + " " + parts[1];
}

// Updates the big hero rectangle text
function updateHomeHeroCard() {
  const mainLine = document.getElementById("home-hero-main");
  const subLine  = document.getElementById("home-hero-sub");
  if (!mainLine || !subLine) return;

  const currentUser =
    (typeof presentacyGetCurrentUser === "function"
      ? presentacyGetCurrentUser()
      : null);

  // Nobody logged in → generic text
  if (!currentUser) {
    mainLine.textContent = "Welcome to Presentacy";
    subLine.textContent  = "Track your presentations and see your progress.";
    return;
  }

  const hour = new Date().getHours();
  let prefix = "Welcome";
  if (hour < 12) prefix = "Good morning";
  else if (hour < 18) prefix = "Good afternoon";
  else prefix = "Good evening";

  const fullName = currentUser.name || currentUser.username || "";
  const first = getFirstName(fullName);

  // Main greeting line
  mainLine.textContent = `${prefix}, ${first}!`;

  // Second line – slightly different for teacher vs student
  if (currentUser.role === "teacher") {
    subLine.textContent =
      "Check your scores, see the leaderboard, and get ready for the next Presentacy.";
  } else {
    subLine.textContent =
      "Check your scores, see the leaderboard, and get ready for your next Presentacy.";
  }
}

// Updates the circle avatar + name badge on the left
function updateHomeHeroUserBadge() {
  const avatarEl = document.getElementById("home-hero-avatar");
  const nameEl   = document.getElementById("home-hero-name");
  if (!avatarEl || !nameEl) return;

  const user =
    (typeof presentacyGetCurrentUser === "function"
      ? presentacyGetCurrentUser()
      : null);
  if (!user) return;

  const fullName = (user.name || user.username || "").trim();
  if (!fullName) return;

  // Text under the circle → "First Last"
  nameEl.textContent = getFirstTwoNames(fullName);

  // First letter inside the circle
  const firstLetter = fullName[0];
  if (firstLetter) {
    avatarEl.textContent = firstLetter.toUpperCase();
  }
}

// ========================
// DOMContentLoaded – FINAL
// ========================
document.addEventListener("DOMContentLoaded", () => {
  const page = document.body.getAttribute("data-page") || "";

  // Common for all pages
  if (typeof setupNavHighlight === "function") setupNavHighlight();
  if (typeof setupMobileNav === "function") setupMobileNav();
  if (typeof setupLogoutButton === "function") setupLogoutButton();

  // Home hero (greeting + badge)
  if (typeof updateHomeHeroCard === "function") updateHomeHeroCard();
  if (typeof updateHomeHeroUserBadge === "function") updateHomeHeroUserBadge();

  // If you still want the old navbar greeting, keep this line.
  // If you DON'T want it, just delete this next line:
  // if (typeof renderUserGreeting === "function") renderUserGreeting();

  // Page-specific inits
  if (typeof initHomePage === "function") initHomePage();
  if (typeof initGeneratorPage === "function") initGeneratorPage();
  if (typeof initPresentacyWallPage === "function") initPresentacyWallPage();

  if (page === "login" && typeof initPresentacyLoginPage === "function") {
    initPresentacyLoginPage();
    return;
  }

  if (page === "leaderboard" && typeof initLeaderboardPage === "function") {
    initLeaderboardPage();
  }

  if (page === "videos" && typeof initVideoReactions === "function") {
    initVideoReactions();
  }
});



